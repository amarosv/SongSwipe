import 'package:flutter/material.dart';

class CardTrackWidget extends StatelessWidget {
  const CardTrackWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Placeholder();
  }
}